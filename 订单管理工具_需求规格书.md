# 订单跟踪工具 需求规格书

版本：v0.2（2026-09-08，Q1～Q4 已确认）
状态：**部分确认** —— Q1～Q4 已确认并写入正文；Q5～Q12 仍为假设，待后续确认
适用范围：单文件本地网页工具，个人使用

---

## 1. 产品定位

一句话：跟踪供应商的机械零件加工订单，从下单到完成的全过程，让我每天 3 分钟内知道哪些订单要延误、该催谁。

**明确不做的事：**

- 不是 ERP，不管库存、财务、发票、对账、成本核算
- 不是电商系统，无收货地址、无支付、无退款、无物流单号、无购物车
- 不是多人协作平台，无登录、无账号、无权限分级、无操作审计
- 不做工艺路线编排，不管刀具、工时定额、机床排程
- 不做图纸管理，不上传、不预览、不关联 CAD 文件

使用者：1 人（我自己），机械工程师，负责零件加工订单的跟进与协调。
使用地点：办公室台式机为主，偶尔用手机查看。

---

## 2. 术语表

| 术语 | 本文档中的含义 | 备注 |
|---|---|---|
| 订单PO | 一次采购需求，对应一个PO号，示例PO6101202609091123，包含一批物料，图号 + 数量 + 交期 | 不是采购订单 |
| 图号 | 零件图纸编号 | ⚠️ 格式待确认 |
| 图版 | 图纸版本号 | 示例用 A.1/B.3/C.4 |
| 供应商| 承担加工的实体，可能是内部车间或外协厂 | |
| 逾期 | 当前日期 > 承诺交期，且状态不是"已完成"或"已取消" | |
| 临期 | 承诺交期在今天起 3 个自然日内（含今天），且未完成 | |

**术语使用规则：** 界面上一律使用本表中的中文原文，禁止替换为英文、禁止替换为同义词。字段名以本表为准。

---

## 3. 项目宪法（长期约束，每轮开发都适用）

### 技术形态

- 单个 `index.html` 文件，CSS 与 JS 全部内联，不拆分为多文件
- 双击即可在 Edge / Chrome 打开，不需要任何本地服务器
- **不引入任何需要联网加载的资源，不向云端/互联网上传或下载数据**：无 CDN 链接、无 Google Fonts、无外部图标库、无外链图片，保证完全断网可用
- 不使用 React / Vue / Angular / Svelte 等前端框架
- 不使用构建工具：无 webpack、无 vite、无 `npm install`、无打包步骤
- 不使用 TypeScript，只用原生 JavaScript，且限定在 ES2020 以内语法

### 数据存储

- 全部数据存于浏览器 `localStorage`，主键名 `ORDER_TOOL_DATA`
- 必须提供"导出备份"和"导入备份"两个常驻按钮，位于页面顶部固定栏，任何视图下都可见可点
- 每次数据变更后立即写入 `localStorage`，不依赖手动保存按钮

### 界面与文案

- 全中文界面：字段名、状态名、按钮名、提示文案一律中文
- 字段命名使用本规格书原文，禁止翻译为英文、禁止自行改名
- 简洁优先于美观：不加动画、不加过渡效果、不加装饰性渐变、不加阴影堆叠
- 表格正文字号不小于 14px

### 协作规则（对开发者的行为约束）

- 不要增加本规格书未提及的任何功能
- 不要修改已在本规格书中确认的字段名、状态名、枚举值
- 不要"顺手优化"我没让你改的部分
- 每次修改前，先完整读取 `index.html` 全文，再动手
- 修改完成后，列出：改动了哪些位置、每处改动对应规格书的哪一节
- 遇到规格书内部矛盾、或规格书未覆盖的细节时：**停下来问我，不要自行假设**
- 报告"已完成"之前，先按第 9 节验收步骤自测，并逐条报告实际结果

---

## 4. 数据模型

### 4.1 数据模型

一个订单（PO）包含多条物料明细。每条物料明细独立跟踪状态与交期。

#### 订单级字段

| 字段名 | 类型 | 必填 | 约束与允许值 |
|---|---|---|---|
| 订单号 | 文本 | 是 | 采购需求的唯一标识。格式示例 `PO6101202609081256`。保存时去除首尾空格 |
| 订单名称 | 文本 | 是 | 最长 50 字符 |
| 供应商 | 文本 | 是 | 自由填写供应商名称 |
| 优先级 | 单选 | 是 | 枚举：高 / 中 / 低，默认"中" |
| 下单日期 | 日期 | 是 | 格式 YYYY-MM-DD，允许填写过去的日期（用于补录历史订单） |
| 备注 | 长文本 | 否 | **多行输入框**，最长 500 字符，允许换行 |
| 创建时间 | 时间戳 | 系统 | 记录创建时自动写入，界面不显示、不可编辑 |
| 最后修改时间 | 时间戳 | 系统 | 每次保存时自动更新，列表中显示为"X 天前" |

#### 物料明细字段（每个订单包含一条或多条）

| 字段名 | 类型 | 必填 | 约束与允许值 |
|---|---|---|---|
| 零件名称 | 文本 | 是 | 最长 50 字符 |
| 图号 | 文本 | 是 | 格式示例 `L201A002E3`、`L350A02ASW` |
| 图版 | 文本 | 是 | 自由填写，格式为大写字母 + 点 + 数字，如 `A.1`、`B.23`、`C.4` |
| 数量 | 整数 | 是 | 最小 1，最大 999999，不允许小数 |
| 承诺交期 | 日期 | 是 | 格式 YYYY-MM-DD，允许填写过去的日期 |
| 当前状态 | 单选 | 是 | 枚举见 4.2，默认"待排产" |
| 检验结论 | 单选 | 否 | 枚举：合格 / 不合格 / 待判定。仅当当前状态为"待检"时可编辑 |
| 状态变更历史 | 数组 | 系统 | 见 4.3，不可直接编辑 |

显示界面层级：
一级显示信息：订单号 | 订单名称 | 供应商 | 当前状态（各物料中优先级最高的状态汇总）| 最近承诺交期
二级显示信息（展开后显示该订单下所有物料明细）：零件名称 | 图号 | 图版 | 数量 | 承诺交期 | 当前状态 | 检验结论 | 备注
使用可折叠的形式，初步展示一级显示信息，通过折叠展开按钮显示二级详细信息

### 4.2 状态集合与流转规则

状态枚举（共 6 个，顺序即界面下拉框顺序）。以下状态适用于每条物料明细，同一订单内不同物料可处于不同状态：

1. 待排产
2. 加工中
3. 外协中
4. 待检
5. 已完成
6. 已取消

允许的流转：

| 从 | 可到 |
|---|---|
| 待排产 | 加工中、外协中、已取消 |
| 加工中 | 待检、外协中、已取消 |
| 外协中 | 待检、加工中、已取消 |
| 待检 | 已完成、加工中（返工）、外协中（退回外协）、已取消 |
| 已完成 | 已取消（需二次确认） |
| 已取消 | 待排产（复活，需二次确认） |

**禁止的流转：** 上表未列出的组合一律禁止。特别是禁止 `已完成 → 待排产`。

实现要求：状态下拉框中**只显示当前状态允许跳转到的选项**，不允许选中的状态根本不出现在下拉框里。不要做成"允许选中后再弹错误提示"。

视觉要求：每种状态配颜色标记，但**必须同时显示状态文字**，禁止只用颜色区分（考虑色觉异常使用者与黑白打印）。

- 逾期：红底白字
- 临期（3 日内）：黄底黑字
- 正常推进：无底色
- 已完成：绿底白字
- 已取消：灰底、灰字

### 4.3 状态变更历史

每次物料明细的"当前状态"发生变化时，自动向该物料的历史数组追加一条记录：

```
{ "时间": "2026-09-08T09:14:00", "从": "待排产", "到": "外协中", "操作备注": "" }
```

规则：

- 追加即写入，历史记录**不删除、不修改**
- 修改状态时弹出一个可选的单行备注框（可留空直接确认），内容存入"操作备注"
- 历史在物料详情中显示为时间线，最新在上
- 首次创建订单时，每条物料写入一条 `{ "从": "（新建）", "到": "待排产" }`

用途说明：这份历史是后续做"各环节耗时""外协平均延误天数"分析的唯一数据来源。若只存当前状态，这些数据将永久无法追溯。**因此本字段为必需项，任何情况下不得省略或简化。**

### 4.4 真实样本数据

一级显示信息示例（订单级）：

| 订单号 | 订单名称 | 供应商 | 当前状态（汇总） | 最近承诺交期 |
|---|---|---|---|---|
| PO01202609061256 | 密封组件采购 | 飞特 | 加工中 | 2026-10-05 |
| PO01202604232544 | 防水组件采购 | 景星 | 待排产 | 2026-06-15 |
| PO01202511235214 | --- | 秦楚 | 待排产 | 2025-12-30 |

二级显示信息示例（展开后显示该订单下各物料明细）：

| 零件名称 | 图号 | 图版 | 数量 | 承诺交期 | 当前状态 |
|---|---|---|---|---|---|
| 密封件1 | L258A0155 | A.3 | 35 | 2026-10-05 | 加工中 |
| 长条件 | U524F5215 | B.3 | 35 | 2026-10-01 | 待排产 |
| 黄桃罐头 | P5158EW12 | C.1 | 55 | 2026-09-28 | 待排产 |

### 4.5 数据结构示意

```json
{
  "version": 1,
  "orders": [
    {
      "id": "o_1725753600000_a1b2",
      "订单号": "PO2501202605220125",
      "订单名称": "泄压工装需求",
      "供应商": "柏拉图",
      "优先级": "高",
      "下单日期": "2026-08-20",
      "备注": "客户要求随货提供材质证明书，表面处理为阳极氧化黑色，不得有划伤",
      "创建时间": "2026-08-20T10:30:00",
      "最后修改时间": "2026-09-07T16:20:00",
      "物料明细": [
        {
          "零件名称": "真空腔体法兰",
          "图号": "LM-3A-2201",
          "图版": "C.2",
          "数量": 4,
          "承诺交期": "2026-09-05",
          "当前状态": "外协中",
          "检验结论": null,
          "状态变更历史": [
            { "时间": "2026-08-20T10:30:00", "从": "（新建）", "到": "待排产", "操作备注": "" },
            { "时间": "2026-08-22T09:00:00", "从": "待排产", "到": "外协中", "操作备注": "内部产能不足" }
          ]
        }
      ]
    }
  ]
}
```

说明：

- `id` 为系统内部唯一键，**与业务上的"订单号"分离**。这样订单号录错时可以修改，不会导致历史记录与统计断裂
- JSON 业务字段名使用中文（便于导出文件人工阅读与排查问题）；`id`、`version`、`orders` 等结构键使用英文
- `version` 字段用于未来数据结构升级时做兼容迁移，首版固定为 1
- 一个订单包含多条物料明细，存储在 `物料明细` 数组中，每条物料独立跟踪状态与交期
---

## 5. 使用场景

> ⚠️ 本节多数条目为编写者的假设，需使用者逐条确认或改写。

| 编号 | 场景 | 频率 | 设备 | 由此产生的设计需求 |
|---|---|---|---|---|
| S1 | 每周一项目会前，快速确认今天有哪些订单要逾期、该催谁 | 每天 1 次，约 3 分钟 | 办公室台式机 | 默认打开"今日预警"视图，不做任何多余步骤 |
| S2 | 梳理出投样采购需求后，按照模板整理物料BOM， 后录入新订单 | 每天 0～5 次 | 办公室台式机 | 录入表单字段支持按照固定模板进行导入 |
| S3 | 状态变化时更新（外协件到货、检验完成等） | 每天多次 | 办公室台式机 | **在列表中直接改状态，不进入详情页，不弹窗修改**；改状态即触发 4.3 的历史记录 |
| S4 | 车间现场临时查某个订单的交期与状态 | 每周数次 | 手机 | 响应式布局；窄屏下表格可横向滚动；订单号、交期、状态三列优先保证可见 |
| S5 | 汇总，向主管或项目经理报告本月完成与逾期情况 | 每周 1 次 | 办公室台式机 | 月度统计视图 + 导出 CSV。⚠️ 待确认主管习惯的列与格式 |
| S6 | 换电脑或清理浏览器缓存后恢复数据 | 不定期 | — | 导出/导入 JSON；导入时必须明确选择覆盖或合并（见 7.4） |
| S7 | 某订单要延期，回头查它卡在哪个环节多久了 | 不定期 | — | 订单详情显示状态变更历史时间线 |

**未列入的场景（明确不在首版范围内）：** 多人同时编辑、与邮件系统对接、自动提醒推送、与 ERP/MES 同步、附件上传、数据云端同步。

---

## 6. 视图清单

### 视图一：今日预警（默认视图，打开工具即显示）

**目的：3 分钟内知道今天该催谁。**

内容，按以下顺序分三组显示（以物料明细为单位检查交期）：

1. **已逾期**：物料的承诺交期 < 今天，且该物料状态不是"已完成/已取消"。按逾期天数降序（逾期最久的排最上）
2. **今日到期**：物料的承诺交期 = 今天，且该物料状态不是"已完成/已取消"
3. **3 日内到期**：物料的承诺交期在今天之后 3 个自然日内，且该物料状态不是"已完成/已取消"

每组显示组标题与条数，例如"已逾期（3）"。组内无数据时该组整体不显示。三组全部为空时，显示一行"当前无预警订单"。

每条预警按物料明细显示，同时标注所属订单：订单号、供应商、零件名称、图号+图版、数量、承诺交期、逾期/剩余天数、当前状态。

行内操作：

- 状态下拉框，可直接改该物料的状态（遵守 4.2 流转规则）
- "详情"链接，展开该订单的编辑区

**不提供：** 新增订单按钮（新增只在视图二）、批量操作、任何图表、任何汇总数字。

### 视图二：全部订单

**目的：查找、录入、修改。**

内容：订单列表，默认按"下单日期"降序。每个订单行显示一级信息：订单号、订单名称、供应商、当前状态（汇总）、最近承诺交期、最后修改时间。

顶部工具条：

- 搜索框：对"订单号""订单名称""供应商""零件名称""图号""备注"做不区分大小写的模糊匹配，**输入即筛选**（不需要点搜索按钮）
- 状态筛选：多选下拉，默认全选（匹配订单内任一物料的状态即显示该订单）
- 优先级筛选：多选下拉，默认全选
- "新增订单"按钮

行操作：点击订单行，**在该行下方行内展开**，显示该订单下所有物料明细表格（零件名称、图号、图版、数量、承诺交期、当前状态、检验结论）。编辑区含 4.1 全部可编辑字段（订单级 + 物料级）+ 各物料的状态变更历史时间线 + "删除订单"按钮（需二次确认）。不跳页、不弹全屏模态。

排序：点击"下单日期""最近承诺交期"列头可切换升序/降序。

分页：每页 50 条订单，底部显示"共 N 条订单 / 第 X 页 / 共 Y 页"。

### 视图三：月度统计

**目的：月底交报表。**

> ⚠️ 本视图列入首版但仅实现基础内容，待 S5 场景确认后细化。

内容：

- 月份选择器（默认当前月）
- 数字卡片：本月新增订单数、本月完成数、本月逾期数、当前在制数（状态非"已完成/已取消"的总数）
- 按供应商分组的表格：供应商、订单数、已完成数、逾期数
- "导出本月数据为 CSV"按钮：导出当月全部订单明细，**编码为 UTF-8 带 BOM**（保证 Excel 直接双击打开中文不乱码）

**不做：** 任何图表、趋势线、饼图、同比环比。

### 全局常驻元素

页面顶部固定栏显示：

- 工具名称
- 三个视图的切换标签
- "导出备份"按钮
- "导入备份"按钮
- 数据条数提示，如"共 128 条订单，最后保存于 09-08 16:20"

---

## 7. 边界情况与异常处理

### 7.1 无数据时

- 今日预警视图：显示"当前无预警订单"
- 全部订单视图：显示"还没有任何订单"，并给出一个醒目的"新增第一条订单"按钮
- 月度统计视图：各项数字显示 0，不显示错误信息

### 7.2 录入校验

必填项为空或格式非法时：**阻止保存**，在对应字段下方显示红色中文提示，焦点自动移到第一个出错字段。

提示文案（要求原文照用）：

| 情况 | 提示文案 |
|---|---|
| 必填项为空 | `请填写{字段名}` |
| 订单号重复 | `订单号 {值} 已存在，请检查是否重复录入` |
| 数量非正整数 | `数量必须是不小于 1 的整数` |
| 日期格式非法 | `日期格式应为 2026-09-08` |
| 字段超长 | `{字段名}最多 {N} 个字符` |

**禁止使用浏览器原生英文校验提示**（如 "Please fill out this field"）。实现方式：表单元素上加 `novalidate`，自行校验。

### 7.3 允许但需标记的情况

- 承诺交期早于今天且状态未完成：**允许保存**（补录历史订单需要），但在所有视图中该行标红并计入逾期
- 下单日期晚于承诺交期：允许保存，但保存时弹出一次确认"承诺交期早于下单日期，是否确认？"
- 数量为 1 的大件、数量很大的标准件：不做特殊处理

### 7.4 导入 JSON 的处理（重点）

点击"导入备份"并选择文件后，**不得直接覆盖现有数据**。必须先弹出对话框，显示：

- 当前本地数据条数
- 待导入文件的数据条数
- 待导入文件的 `version` 值与导出时间（如文件中有）
- 两个明确选项：
  - **覆盖**：清空现有数据，用文件内容替换。需在输入框中手动键入"确认覆盖"四个字，确定按钮才可用
  - **合并**：以"订单号"为键。文件中已存在的订单号 → 用文件里的整条记录替换；文件中不存在的订单号 → 作为新记录加入；本地有而文件中没有的订单号 → 保留不动
- 一个取消按钮

导入动作执行前，自动把当前数据另存为一份带时间戳的备份，存入 `localStorage` 的第二个键 `ORDER_TOOL_BACKUP_AUTO`，**只保留最近 3 份**（超出时删最旧的），以便误操作后找回。

导入完成后显示结果：`导入完成，覆盖 X 条，新增 Y 条，保留 Z 条，当前共 N 条`。

文件格式非法时（不是合法 JSON、缺 `orders` 键、`version` 高于当前程序支持的版本）：**不做任何修改**，提示"文件格式不正确，现有数据未改动"。

### 7.5 数据量与存储容量

- 目标：1000 条订单以内操作流畅，录入、搜索、改状态的响应时间 < 200ms
- `localStorage` 单域名容量上限约 5MB。按每条订单（含状态历史）约 600 字节估算，1000 条约 600KB，2000 条约 1.2MB，在安全范围内
- 写入 `localStorage` 失败时（超出配额、被浏览器策略禁止、无痕模式限制）：**不得静默失败**，弹出提示"数据保存失败，请立即导出备份"，并在内存中保留数据以供导出
- 每次启动时校验数据可读性。若 JSON 解析失败：**不覆盖原数据**，提示"本地数据已损坏，请尝试导入备份"，并提供一个"查看原始数据"入口（把原始字符串显示在文本框中供手动复制抢救）

### 7.6 删除

- 删除单个订单需二次确认，确认框中必须显示该订单的订单号与零件名称
- **状态为"已完成"的订单不允许删除**，只允许改为"已取消"（保护历史数据）
- 不提供"清空全部数据"按钮
- 不提供批量删除

---

## 8. 视觉与交互约束

- 配色：白底 + 深灰文字，仅用状态色（红/黄/绿/灰）做强调，不使用大面积彩色块
- 字体：使用系统默认中文字体栈（`"Microsoft YaHei", "PingFang SC", sans-serif`），不引入任何网络字体
- 正文字号不小于 14px，**表格行高不小于 36px**（满足车间手机触控点击，避免误点相邻行）
- 表格列宽：订单号、状态、日期列固定宽度；零件名称、备注列自适应并允许换行
- 状态列必须同时有颜色和文字，禁止只用颜色
- 响应式断点：视口宽度 < 768px 时，表格改为可横向滚动，工具条按钮自动换行，固定顶栏不遮挡内容
- 所有交互反馈即时显示，不加动画过渡
- 键盘：录入表单内 Tab 顺序与 4.1 字段顺序一致；编辑状态下 Ctrl+S 等同于点击保存；Esc 关闭弹层与行内展开区

---

## 9. 验收标准

开发者在报告完成前，必须用第 4.4 节的真实样本数据（若尚未提供，则用 4.5 节的示意数据）逐条自测，并报告每条的**实际结果**，不得只回答"已完成"。

| 编号 | 验收步骤 | 通过标准 |
|---|---|---|
| A1 | 双击 index.html 在 Edge 中打开；随后关闭网络再刷新一次 | 两次均正常显示"今日预警"视图，控制台无报错 |
| A2 | 录入样本数据中的全部订单，然后刷新页面 | 每条都能保存；刷新后数据完整仍在 |
| A3 | 录入一条订单号与已有记录相同的订单 | 阻止保存，显示中文重复提示，不产生新记录 |
| A4 | 数量分别填 0、1.5、abc、-3 | 四种情况均阻止保存，提示"数量必须是不小于 1 的整数" |
| A5 | 新建一条订单（状态为待排产），展开状态下拉框 | 下拉框中**不出现**"已完成"和"待检"（这两个流转被禁止） |
| A6 | 把某物料状态改为"外协中"，点保存 | 保存成功，无额外校验阻止 |
| A7 | 把该物料状态改为"待检"，保存，再展开详情查看历史 | 保存成功；状态变更历史中新增一条记录，时间、从状态、到状态均正确 |
| A8 | 把某订单承诺交期改为昨天，状态保持"加工中" | 该订单出现在"今日预警"的"已逾期"组，行标红，逾期天数显示为 1 |
| A9 | 把该订单状态改为"已完成" | 该订单立即从"今日预警"中消失，即使交期已过 |
| A10 | 在搜索框输入某图号的一部分，再清空 | 输入时列表实时筛选出匹配项；清空后恢复全部记录 |
| A11 | 点击"导出备份" | 下载得到 .json 文件，文件名含日期；用记事本打开可见中文字段名与全部数据 |
| A12 | 手动清空 localStorage 后刷新，再导入刚才的备份，选择"合并" | 数据完整恢复，条数与导出前一致，状态变更历史也完整保留 |
| A13 | 在已有数据的情况下再次导入同一份备份，选择"覆盖" | 必须先键入"确认覆盖"才能点确定；完成后条数不变 |
| A14 | 导入一个非 JSON 的文本文件 | 提示格式不正确，现有数据条数与内容均未发生任何变化 |
| A15 | 切换到"月度统计"，导出 CSV，用 Excel 双击打开 | 中文不乱码，列名与数据正确 |
| A16 | 把浏览器窗口宽度缩到 400px 左右 | 布局不破裂，表格可横向滚动，所有按钮仍可点击 |
| A17 | 检查 index.html 全文，并查看所在目录 | 不存在任何 `http://` 或 `https://` 开头的外部资源引用；目录下没有产生除 index.html 之外的新文件 |
| A18 | 把某订单改为"已完成"后尝试删除它 | 删除按钮不可用或点击后被拒绝，并提示已完成的订单只能改为已取消 |

---

## 10. 开发轮次划分

按以下顺序分轮开发。每轮完成后按对应验收条目测试通过，再进入下一轮。**禁止一次性实现全部内容。**

| 轮次 | 范围 | 对应验收 |
|---|---|---|
| 第 1 轮 | 第 3 节宪法 + 4.1 字段 + 视图二的"新增/编辑/列表/搜索/分页" + 导出导入 | A1～A4、A11～A14、A17 |
| 第 2 轮 | 4.2 状态流转规则 + 4.3 状态变更历史 | A5～A7、A18 |
| 第 3 轮 | 视图一：今日预警 | A8～A9 |
| 第 4 轮 | 7.5、7.6 的异常处理与删除保护 | A14、A18 及人工抽查 |
| 第 5 轮 | 视图三：月度统计 + CSV 导出 | A15 |
| 第 6 轮 | 第 8 节视觉约束 + 响应式 | A16 |

分轮顺序的依据：第 1 轮先确保"数据能正确存进去、取出来、备份回来"，这是其余一切的基础。若数据持久化有缺陷，第 2 轮之后测出的问题将无法定位到具体层次。

---

## 11. 待确认事项（阻塞项）

以下条目应在开发第 1 轮之前由使用者确认。未确认前，开发者按标注的 ⚠️ 示例值实现，并在交付说明中**明确列出所有按假设实现的位置**。

| 编号 | 待确认内容 | 确认结果 | 影响范围 |
|---|---|---|---|
| Q1 | "订单"具体指什么：机械零件加工订单 / 采购订单 / 其他 | ✅ 已确认：跟踪供应商的机械零件加工订单 | 全文 |
| Q2 | 订单号的真实格式与编码规则 | ✅ 已确认：格式如 `PO6101202609091123`，自由文本 | 4.1、7.2 |
| Q3 | 图号、图版的真实格式 | ✅ 已确认：图号自由格式；图版为大写字母+点+数字（如 A.1、B.23），自由填写 | 4.1 |
| Q4 | 需求方、加工方字段 | ✅ 已确认：不需要需求方和加工方字段，只保留"供应商" | 4.1、视图一、视图二、视图三、7.2 |
| Q5 | 材料是自由填写还是限定牌号清单 | 自由填写 | 4.1 |
| Q6 | 6 个状态是否完整，是否缺少"来料检验""热处理中""装配中"等环节 | 6 个够用 | 4.2 |
| Q7 | 是否存在"同一图号多个订单并行"或"同一订单号分批交付" | 同图号可多单；订单号全局唯一 | 4.1 唯一性校验、7.4 合并键 |
| Q8 | 月度报表中主管要求的具体列与格式 | 未确认，先按视图三实现 | 视图三、A15 |
| Q9 | 录入数据的来源：邮件正文 / Excel 附件 / 纸质单据 / 微信 | 邮件正文，手工转录 | S2，是否需要粘贴自动拆分 |
| Q10 | 车间用手机时是否需要录入（而不只是查看） | 只查看，不录入 | S4、第 8 节 |
| Q11 | 是否需要关联图纸 PDF、检验报告等附件 | 首版不需要 | 范围外，见第 12 节 |
| Q12 | 订单数据是否有公司信息安全/保密要求，能否存放在个人电脑的浏览器中 | 可以 | **全文，若不可则技术方案需整体改变** |

Q12 补充说明：订单号、客户名、图号在多数制造企业中属于内部或保密信息。将其存放在个人设备的浏览器本地存储中，是否符合公司规定，需先向相关部门确认。若不符合，本规格书的技术方案需重新设计（例如限定在公司配发的设备上使用，或改用经批准的存储方式）。

---

## 12. 阶段二候选需求（本版本明确不实现）

记录于此，避免开发过程中被误认为"遗漏的功能"而擅自实现：

- 外协厂档案：独立的供应商实体，含联系人、常用工序、历史准时率
- 各环节耗时统计：依赖 4.3 的状态历史积累 2～3 个月后才有统计意义
- 与 Excel 的双向同步（读入 + 写回）
- 到期前的主动提醒（桌面通知或邮件）
- 数据自动备份到本地文件夹：需引入本地服务，会脱离单文件形态
- 图纸文件关联与在线预览
- 多设备间数据同步
- 粘贴文本自动拆分为字段

---

## 文档维护说明

本规格书是活文档，随开发轮次持续更新，不是写完即冻结的招标书：

- 每轮验收通过后，把本轮新做的决定写回对应章节
- 第 11 节的待确认事项一旦确认，删除 ⚠️ 标记并写入实际值
- 实际使用两周后产生的新需求，**先写入第 12 节**，不直接进入开发范围
- 每次修改在下方变更记录中留一行

变更记录：

- v0.1（2026-09-08）：首版，依据使用者口述需求编写，含 12 项待确认假设，尚未填入真实样本数据
- v0.2（2026-09-08）：Q1～Q4 已确认。主要变更：① 删除"需求方""加工方"字段，只保留"供应商"；② 数据模型改为订单级+物料明细子数组（方案 B），一个 PO 包含多个零件；③ 图版改为自由文本（大写字母.多位数字）；④ 检验结论改为仅当前状态为"待检"时可编辑；⑤ 修正样本数据格式；⑥ 统一表头为"图版"；⑦ 更新视图一/二/三、验收标准 A6/A7、待确认事项表
