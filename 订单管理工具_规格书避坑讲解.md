# 规格书逐层避坑讲解

配套文档：《订单管理工具_需求规格书.md》
用途：说明那份规格书里每个决定在防什么坑，以及你需要改哪里

---

## 0. 为什么分成两个文件

规格书是给 Agent 读的**输入**，讲解是给你读的**教材**。混在一份里有三个坏处：

1. Agent 会把讲解文字也当需求，比如读到"这里容易被做成电商系统"，它可能真去加电商字段
2. 讲解会撑大上下文，挤掉真正的需求内容，加剧失忆
3. 规格书要随开发迭代更新，讲解不用；混在一起每次更新都要小心避开讲解段落

**这本身是第一课：交给 Agent 的文件里只放需求，不放你对需求的评论。**评论写进另一个文件，或者干脆只留在你脑子里。

---

## 1. 我替你做的 12 个假设（请优先处理这一节）

我不知道你的订单号长什么样、有没有外协环节、主管要看什么报表。所以我编了一套看起来合理的。**如果直接拿去开发，你会得到一个"平均的机械加工订单工具"，而不是"你的工具"。**

| 编号 | 我的假设 | 如果猜错会怎样 | 改规格书哪一节 |
|---|---|---|---|
| Q1 | 订单 = 机械零件加工订单 | 若是采购订单，字段要大改：需加供应商、单价、到货批次；可去掉图版、外协 | 4.1 全部、第 1 节 |
| Q2 | 订单号格式 `PO-2026-0147` | 格式假设错不致命，但重复校验和搜索会不准 | 4.1 |
| Q3 | 图号 `LM-3A-2201`，图版 Rev.A～E | 若你的图版用数字（如 -01/-02）或无版本概念，枚举要改 | 4.1 |
| Q4 | 需求方可能是内部部门也可能是外部客户 | 若只有内部部门，"需求方"应改为部门下拉；统计口径也变 | 4.1、视图三 |
| Q5 | 材料自由填写 | 若你常按材料统计或校验牌号，应改成枚举 | 4.1 |
| Q6 | 6 个状态够用 | **这条影响最大**。若缺"来料检验""热处理中"，状态机要重画，且历史记录会出现断层 | 4.2、4.3 |
| Q7 | 订单号全局唯一，同图号可多单 | 若同一订单号会分批交付（很常见），那"订单号"就不能当唯一键，导入合并的策略也要改 | 4.1、7.4 |
| Q8 | 月度报表格式未定 | 做出来的 CSV 主管不认，你还得手工重排 | 视图三 |
| Q9 | 数据从邮件正文手工转录 | 若你实际从 Excel 复制，就该优先做"粘贴自动拆分"，而不是优化手工表单 | S2、第 12 节 |
| Q10 | 手机上只看不录 | 若车间也要录入，响应式要做认真得多，触控目标要更大 | S4、第 8 节 |
| Q11 | 不需要附件 | 若你需要随时调图纸，单文件方案撑不住，要提前规划阶段二 | 第 12 节 |
| Q12 | 数据可以存在个人电脑浏览器里 | **若公司不允许，整个技术方案推翻重来**。这条必须在写代码前确认 | 全文 |

**Q12 是我主动加的风险项，你可能根本没想到。**订单号、客户名、图号在制造企业里多数属于内部或保密信息，存在个人电脑的浏览器本地存储中是否合规，你该先问一下。这不是技术问题，是能不能做的问题。放在第 11 节最后一条，但优先级最高。

---

## 2. 逐层讲解：每段在防什么坑

### 第 1 节 产品定位 —— "不做的事"比"要做的事"信息量更大

我列了 5 条明确排除项。

**为什么有用：** 模型是在做概率补全。你只说"订单管理"，它的分布中心是电商订单系统。你说"不是 ERP、不是电商、不是协作平台、不做工艺路线、不做图纸管理"，等于把几个高密度区域显式排除掉。**负面约束的收窄效率远高于正面描述**——正面描述你要说很多条才能圈定范围，负面排除三五条就能砍掉大半。

机械类比：设计说明书里"本夹具不适用于 XXX 工况"和"适用于 XXX"同样重要，前者防止误用。

你要动的地方：确认这 5 条排除是否都对。特别是"不做工艺路线"——如果你其实想记录每道工序的耗时，这条就得从排除项挪进第 12 节。

### 第 2 节 术语表 —— 我上一轮没提，这次加的

**为什么需要：** 模型有很强的"术语标准化"倾向。你写"图版"，它在代码里会写成 `revision`，界面上显示"版本"。你写"外协"，它会写成"委外""供应商""厂商"。你写"需求方"，它会写成"客户"。

单次漂移无所谓，问题在于漂移之后**你没法精确指代**。你说"把加工方那列往前挪"，它以为你说的是"外协厂"，改了别的地方。术语不统一，你的迭代指令就开始失效。

术语表的作用是把命名权从模型手里拿回来。

机械类比：图面上的名词统一。同一套图纸里一个零件不能一会儿叫"支座"一会儿叫"托架"，否则装配时找不到对应件。

### 第 3 节 项目宪法 —— 四条最容易被忽略但最有用

**① "不引入任何需要联网加载的资源"**

Agent 生成网页时有极高概率引入 Tailwind CDN、Bootstrap CDN、Google Fonts、FontAwesome。因为训练数据里这类写法占多数，是它的"默认手艺"。

后果：断网时白屏、公司网络拦截外链时白屏、CDN 改版后布局崩掉、且这些故障你很难自己排查（页面看起来"就是坏了"）。

**② "限定 ES2020 以内语法，不用 TypeScript"**

TypeScript 需要编译，直接违背单文件原则。新语法特性在某些环境下报错，报错信息你还看不懂。收窄语法范围 = 减少故障面。

**③ "改完列出你改了哪几处，对应规格书哪一节"**

这是把它的输出变成可审计的。否则它说"已完成"，你不知道它动了什么，出问题时无法定位是哪一轮引入的。

机械类比：工艺变更单必须写明变更内容与变更依据，否则没法追溯，出了问题查不到是哪次改动引起的。

**④ "遇到矛盾或缺失时停下来问我，不要自行假设"**

我上一轮说过这是价值最高的一句话。它放在宪法里而不是某次对话里，是因为**它需要跨对话持久生效**——你下次新开一个对话，它还记得这条规矩。

### 第 4 节 数据模型 —— 三个关键决定

#### 决定一：`id` 与"订单号"分离

**不这么做会怎样：** Agent 大概率直接用订单号当主键（数组索引或对象的 key）。

后果两个：

- 订单号录错了想改 → 所有关联数据（状态历史、月度统计）断裂或丢失
- 两条记录订单号相同（现实中会发生，手工录入难免重复）→ 后一条静默覆盖前一条，**你根本不知道数据少了**

这是软件工程里的经典错误：拿业务字段当技术主键。业务字段会变、会重复，技术主键必须不变且唯一。

机械类比：零件的序列号（生产时打的钢印）和图号是两回事。图号会改版、会标错，序列号才是唯一且不变的追溯依据。你不会用图号去查某一件具体零件的热处理记录。

你要确认的：Q7。如果你的业务里同一订单号确实会分批出现多次，那这个分离更加必需。

#### 决定二：现在就存状态变更历史（**整份规格里我最想让你坚持的一条**）

**不这么做会怎样：** Agent 只会存"当前状态"一个字段，因为那是"订单管理"最常见的实现方式。

三个月后你想算"外协平均耗时多少天""哪个环节最容易卡住""哪家外协厂准时率最低"——**数据没了，永久无法追溯，花多少钱都补不回来。**

这是规格里唯一一个"现在不做、以后永远做不了"的决定。其他所有决定后期都能改：字段能加、视图能改、界面能重做。只有历史数据不能追认。

成本极低：一个数组 push，十几行代码。收益是长期的。

机械类比：装配时不预留测量点和传感器安装位，等整机跑起来想测振动，发现没地方装探头，只能拆机改造。**开孔要趁早。**

#### 决定三：字段约束写到"允许值"级别

**不这么做会怎样：**

- 你说"数量"，它可能做成文本框（能输入"4件"）、可能允许小数（0.5）、可能允许负数
- 你说"优先级"，它可能做成自由文本输入。于是三个月后你的数据里同时存在"高""紧急""加急""急""high""★★★"，**再也无法统计**

枚举字段必须给出完整允许值列表。这是让数据"可统计"的前提，也是让 4.3 的历史数据有意义的的前提。

机械类比：公差标注。写"直径 20"和写"直径 20 ±0.01"是两个完全不同的东西。前者没法加工，也没法检验——你的验收标准直接失效。

### 4.4 样本数据 —— 我为什么必须留白

**这是整份文档我唯一坚持不替你写的部分。**

我可以编 5 条看起来合理的数据，但编造的数据有三个害处：

1. 它是"平均值"，你的真实数据不是平均值
2. 你会误以为规格已经完整，跳过确认环节
3. Agent 会按假数据的特征做设计——比如假数据的备注都只有几个字，它就把备注做成单行输入框；而你的真实备注可能写了三行工艺要求

填的时候请**刻意保留脏数据**：有空字段的、日期格式不统一的、备注写了一大段的、图号格式特殊的、数量是 1 的和数量是 5000 的都放进来。

机械类比：拿样件去测，而不是拿一张标注模糊的图纸。而且样件要有代表性——包括边界件、返修件、超差件，不能只挑最漂亮的那个送去检验。

### 第 5 节 使用场景 —— 表格最后一列才是关键

注意我加的最后一列"由此产生的设计需求"。

大多数人写场景只写"什么时候用"，然后就停了。**但场景本身不会自动变成设计。**把场景翻译成设计需求这一步是设计师的工作，不能指望 Agent 替你做——它会做，但做得平庸。

两个例子：

- S1"早上 3 分钟看预警" → 翻译成"默认打开预警视图，不做任何多余步骤"。这一句会阻止 Agent 给你做一个带四个图表的首页 dashboard，然后把预警列表放在第二屏需要点一下才能看到。
- S3"状态变化时更新，每天多次" → 翻译成"在列表中直接改状态，不进入详情页"。少了这一句，它会做成"点详情 → 进编辑页 → 找到下拉框 → 改 → 点保存 → 返回列表"。你一天做十次就想砸键盘。

机械类比：工况表本身不是设计输入，"该工况下要求转速多少、扭矩多少、寿命多少小时"才是。只写"用于高温环境"没法设计，要写"环境温度 80℃，连续运行 8 小时"。

**这一节是整份文档里对工具实用性影响最大的一节，而它现在全是我的假设。请务必改成真的。**

### 第 6 节 视图清单 —— "目的"那一行不是废话

每个视图我都写了"目的"。

**作用：** 当 Agent 遇到你没规定的细节（比如"预警视图要不要显示备注列"），它会回到"目的"去推断：目的是 3 分钟看完 → 备注太长会撑开行高、拖慢扫视 → 不显示。

没有"目的"这一行，它就按平均值处理，把所有字段都堆上去。

另外注意我在视图一里写了"**不提供**：新增订单按钮"。预警视图是浏览视图，加个新增按钮会诱导你在预警页录入，然后录完找不到自己录到哪了，还会误以为那条也在预警里。

机械类比：视图的目的 = 该视图的功能定义。就像"这张剖视图是为了表达内部油路走向"——有了这句话，标注哪些尺寸、剖切位置选在哪，就有了依据。没有这句话，绘图员只能猜。

### 第 7 节 边界与例外 —— 四个最容易出事的

#### 7.4 导入必须选覆盖/合并（**整份规格里第二重要的一条**）

**不写这条会怎样：** Agent 会做成"选择文件 → 直接覆盖 localStorage"。

某天你想找回一条误删的订单，导入了上周的备份。结果上周的备份**覆盖掉了这周的全部数据**。而且不可恢复，因为覆盖已经发生了，你手上没有更新的备份。

所以我要求四层保护：① 先显示两边条数，让你有判断依据 ② 覆盖需手动键入"确认覆盖"四个字 ③ 合并以订单号为键，明确三种情况各怎么处理 ④ 导入前自动另存最近 3 份备份。

机械类比：双重确认的防错设计（Poka-yoke）。像压力机必须双手同时按住两个按钮才能下压——故意让"单手误操作"在物理上变得不可能，而不是靠警告标语。

你要确认的：合并以"订单号"为键是否合适（即 Q7）。如果同一订单号会分批出现多次，这个键就错了，得改成 `id` 或"订单号+批次号"。**键选错，合并就会静默吃掉数据。**

#### 7.5 localStorage 会失败，而且默认不告诉你

新手不知道的事：localStorage 在无痕模式下可能不可用、磁盘满时写入失败、企业组策略下被禁用、超过配额时抛异常。

**默认情况下这些失败是静默的**——代码执行了，没报错到界面上，你以为存了，其实没存。等你哪天刷新页面发现数据全没了，已经来不及。

所以我要求：写入失败必须弹提示。

另外我给了容量估算（每条约 600 字节，1000 条约 600KB，上限 5MB）。这个数字不是随手编的，是按 4.5 的示意数据数出来的。给数量级估算能让 Agent 判断要不要做分页、要不要做懒加载，而不是拍脑袋决定。

#### 7.6 已完成的订单不许删除

数据保护原则：**历史数据只增不减，用状态标记代替物理删除。**

不这么做的后果：某天手滑删了一条已完成的订单，月底统计的"本月完成数"对不上，而且你查不出是哪条没了——因为它已经不存在了，连痕迹都没留。

机械类比：不合格品不能直接扔进废料桶了事，要挂不合格标签、进隔离区、留记录。物理销毁等于销毁证据。

#### CSV 必须 UTF-8 带 BOM

中文用户的必踩坑。不带 BOM 的 UTF-8 CSV，用 Excel 双击打开是**满屏乱码**。Agent 生成 CSV 导出时默认不会加 BOM，因为它训练数据里的英文场景不需要。

你不需要懂 BOM 是什么，你只需要在规格里写这一句。

**这就是规格书的价值：把别人踩过的坑，变成你的一行要求。**你不必自己踩一遍。

### 第 8 节 视觉约束 —— 两条有工程依据的

**① 状态色必须配文字**

两个理由，都是硬的：

- 约 8% 的男性有红绿色觉异常，红绿区分对他们不可靠
- 你可能要打印出来贴在车间墙上。黑白打印机一打，红黄绿全变成深浅不一的灰，**颜色编码彻底失效**

机械类比：图纸上的公差不能只靠颜色或线型区分，必须有文字或符号标注。

**② 表格行高不小于 36px**

这条来自场景 S4（车间用手机查）。手指点击目标小于 36px 就容易点错行，而点错行的后果是**改错订单的状态**——不是视觉上难看，是数据出错。

注意这条和第 5 节的联动：**视觉参数不是审美选择，是场景推导出的工程要求。**这就是为什么第 5 节要先写。

### 第 9 节 验收标准 —— A17 最值得你注意

A17：检查全文不存在任何 http/https 外部引用，且目录下没有产生额外文件。

**为什么要单独列一条：** 这是验证第 3 节宪法有没有被真正遵守。Agent 会答应你"不引外链"，然后在生成代码时手滑加一个 Tailwind CDN，因为那是它最熟的写法。

**口头承诺不可信，要有一条可机械检查的验收项。**

同理，A1 里我写了"关闭网络再刷新一次"——这是一条你可以亲手做的、成本极低的物理验证，比读代码可靠得多。你不需要看懂代码，只需要断网刷新看它白不白屏。

验收条目的写法原则：**每条都是"做一个具体动作 → 看一个可观察结果"**。不写"确保代码质量良好""界面美观"这种没法判定的话。

机械类比：检验规程。每条都要有明确的检测方法和合格判据，不能写"外观良好、运转正常"。写这种话的检验规程等于没有检验规程。

### 第 10 节 分轮次 —— 轮次与验收条目绑定

注意每一轮都标了对应的验收编号。这样每轮结束你只测那几条，不用把 18 条全跑一遍。

第 1 轮的顺序是刻意安排的：先做"能存、能读、能导出"，**故意不做状态流转**。

因为状态流转依赖数据能正确持久化。如果存取有 bug，你在第 2 轮测状态流转时出现的问题，会分不清是流转逻辑错了还是存储错了。**一次只引入一层变量，故障才能定位。**

机械类比：先做部件试验，再做子系统联调，最后整机试车。顺序反了，故障定位成本翻倍——整机试车出问题，你得把每一层都拆开排查。

### 第 11 节 待确认事项 —— 标 ⚠️ 而不是直接写死

我把 12 个假设单独列成一节，并标了 ⚠️。

**为什么不直接把假设写进正文就算了：** 写进正文，假设就变成了"事实"，你看规格书时不会停下来质疑它。单独列一节并标记，是逼你（和 Agent）都看到"这里还没定"。

规格书里还有一个细节：第 11 节开头写了"未确认前，开发者按 ⚠️ 示例值实现，并在交付说明中**明确列出所有按假设实现的位置**"。

这句是给 Agent 的指令，作用是：即使你懒得逐条确认就开工，它交付时也会给你一份"我猜了这些地方"的清单。**把隐性假设变成显性清单**，你事后排查就有了靶子。

### 第 12 节 阶段二候选 —— 一个反直觉的用法

这节主要不是给你看的，**是给 Agent 看的**。

原因：当你说"帮我完善一下这个工具"，Agent 会扫描上下文寻找"可以加的东西"。如果阶段二的需求（外协厂档案、耗时统计、Excel 同步）只存在于对话历史里，它很可能认为这是你**漏掉的**、应该主动帮你补上的功能，然后自作主张实现了。

把它写进规格书并明确标注"本版本不实现"，等于给它划了一条清晰边界：这些我知道，我故意不做。

机械类比：设计说明书里的"本期不做、下期规划"章节。那不是废话，是防止施工队把二期的活提前干了——而且干得还不符合二期的接口要求，等真到二期时反而要拆掉重做。

---

## 3. 这份文档里最值钱的三条（如果只能记三条）

1. **状态变更历史（4.3）** —— 唯一一个"现在不做，以后永远补不回来"的决定。成本十几行代码。
2. **导入选覆盖/合并（7.4）** —— 最容易让你一次性丢光全部数据的地方，而且丢得悄无声息。
3. **使用场景要翻译成设计需求（第 5 节最后一列）** —— 决定工具好不好用的不是功能多少，是这一列写没写。

其余的都是可以事后补的，这三条不行。

---

## 4. 建议你现在做的三件事（按顺序，约 1 小时）

| 顺序 | 做什么 | 预计时间 | 为什么排这个位置 |
|---|---|---|---|
| 1 | 填 4.4 真实样本数据（5～8 条，保留脏特征） | 15 分钟 | 只有你能做，且影响字段设计的所有细节 |
| 2 | 过一遍第 11 节的 12 个待确认项，逐条改成真的 | 20 分钟 | Q6（状态集合）和 Q7（订单号唯一性）会改动数据结构，必须在写代码前定 |
| 3 | 重写第 5 节使用场景，特别是最后一列 | 20 分钟 | 决定工具实用性，现在全是我的假设 |

做完这三件事，这份规格就从"AI 编的平均值"变成"你的设计文件"。那时候再开始第 1 轮开发。

**顺序不要颠倒。**先填样本数据，你看着真实数据过 Q1～Q12 会容易得多；先改场景，你会发现有些字段根本用不上、有些字段还缺。

---

## 5. 附：第一轮可以直接复制的 prompt

等你把上面三件事做完，新建一个文件夹放好规格书，然后在 VS Code 里对 Agent 说（或直接对我说）：

```
我要做一个个人用的订单跟踪工具。

请先完整读取当前目录下的《订单管理工具_需求规格书.md》全文，读完后不要急着写代码。

本轮只做规格书第 10 节的【第 1 轮】，范围是：
- 第 3 节项目宪法的全部技术约束
- 4.1 的字段定义与 4.5 的数据结构
- 视图二（全部订单）的新增、编辑、列表、搜索、分页
- 导出 JSON 备份、导入 JSON 备份（含 7.4 的覆盖/合并选择与自动备份）

本轮明确不做：状态流转规则、状态变更历史、今日预警视图、月度统计、响应式适配。
这些在第 2～6 轮，本轮即使你觉得应该加也不要加。

开始前请先回答我三个问题：
1. 规格书第 11 节里，哪些待确认项会直接影响本轮实现？请列出来。
2. 你打算把文件建成什么样（文件名、大概的代码结构分区）？
3. 有没有你认为规格书写得矛盾或无法实现的地方？

我确认你的回答之后，你再开始写代码。

完成后按第 9 节的 A1～A4、A11～A14、A17 逐条自测，报告每条的实际结果。
```

这段 prompt 里有几个刻意的设计，值得你注意：

- **"读完后不要急着写代码"** —— 掐断它读完就动手的惯性
- **明确列出不做什么** —— 比只说做什么更能防止跑偏
- **要求它先回答三个问题** —— 第 1 问是把隐性假设逼出来，第 3 问是给它一个"提出异议"的合法通道（不问的话它会自己猜着实现）
- **"我确认之后你再开始"** —— 把决策权留在你手上，这是你是总设计师的具体体现
- **验收条目直接点名** —— 不给它"我觉得做完了"的空间

---

## 6. 最后一句

这份规格书我写得很细，但**它现在是 60 分，不是 90 分**。

差的 30 分不在文档里，在你的真实数据和真实使用习惯里。你填完样本数据、改完场景、确认完 12 个假设，它能到 80 分。剩下 10 分要等你真用两周，把不顺手的地方记下来，补进第 12 节，才能到 90 分。

**不存在一次写到 100 分的规格书**，机械产品的设计说明书也不是这么来的。别追求一步到位，追求每轮都往上涨一点。
